function Navbar() {
  return (
    <header>
      <h2>OMNIA MART</h2>

      <nav>
        <a href="#">Home</a>
        <a href="#">Products</a>
        <a href="#">About</a>
      </nav>

      <button>Cart</button>
    </header>
  );
}

export default Navbar;